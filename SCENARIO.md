# Scenario: healthy reference

The `main` branch is the golden reference system. All deterministic checks should pass.

## Preflight

```bash
pnpm verify
```

Expected result: exit code 0.

Use this branch to understand the application or to compare a repaired demo branch with the intended behavior.

